public class SafetyZoneSquare extends Square{
    /**
     * A constructor that initialize the square as safety zone.
     * @param color Represents a string with the square's color.
     */
    public SafetyZoneSquare(String color,int positiononboard){
        super(color,positiononboard);
    }
}